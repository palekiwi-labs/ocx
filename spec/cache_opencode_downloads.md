---
status: todo
branch: custom-base-image
---

Cache opencode binary downloads somewhere so we do not need to re-download every time we build with the same version on top of the default or custom base

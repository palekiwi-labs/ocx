---
status: todo
---

# Research opencode server

---

I would like to be able to integrate `opencode` in scripts and shell commands.
Example: a shell command that asks AI to analyze a file and print a report.

What options does `opencode` provide us with achieve this?
How does `ocx` fit in with this need?
Are there any features we need to add to `ocx` to facilitate this feature?

## Resources

OpenCode documentation is available in: `.agents/master/ref/docs/opencode/`
`opencode` source code is available in: `.agents/master/ref/repos/anomalyco/opencode/`
